const r=require('./src/routes/screenerRoutes'); r.stack.forEach((layer, idx)=>{ if(layer.route){ console.log(idx, Object.keys(layer.route.methods).join(', '), layer.route.path); layer.route.stack.forEach((mw, mi)=>{ console.log('  mw['+mi+'] name='+mw.name); console.log(mw.handle ? mw.handle.toString().split('\\n').slice(0,10).join('\\n') : mw.toString().split('\\n').slice(0,10).join('\\n')); console.log('---'); }); } else { console.log(idx, 'no route', layer.name); }});
